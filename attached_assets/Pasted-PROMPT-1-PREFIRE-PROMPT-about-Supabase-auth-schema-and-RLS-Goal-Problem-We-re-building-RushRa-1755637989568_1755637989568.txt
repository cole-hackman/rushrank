PROMPT 1 — PREFIRE PROMPT about Supabase auth, schema, and RLS

Goal / Problem
We’re building RushRank (fraternity rush app). First milestone: create a secure backend foundation. Deliverables:

Supabase project configured (Auth: magic link + optional Google).

Postgres schema for chapters, memberships, pnms, rounds, votes, events, notes.

Row‑Level Security (RLS) so members only see their chapter’s data; admins have elevated rights.

FastAPI middleware to verify Supabase JWT via JWKS and attach user_id.

Minimal API endpoints to prove the system: GET /api/me, POST /api/rounds/{id}/votes, GET /api/pnms.

Replit env configured with Supabase keys.

Research the codebase first

Inspect current repo structure in Replit. Identify if FastAPI is already set up. Find existing env usage, DB client, and any auth logic.

List what exists vs. missing (brief). Don’t change code yet; just report findings.

Assess issues & propose plan

Confirm Python version, dependency manager, and how we’ll store secrets in Replit.

Plan to add a supabase folder with SQL migrations or a single schema.sql.

Plan to add JWT verification middleware and a db.py module (async Postgres).

Plan RLS policies referencing memberships for access control.

Present a step‑by‑step plan; get my approval before coding (summarize in bullets).

Break into steps
A. Supabase setup (outside code)

Create project; enable Auth (Email OTP/Magic link; optional Google).

Create service role + anon public keys.

Copy JWKS URL: https://<PROJECT>.supabase.co/auth/v1/keys.

Create storage bucket for PNM photos (optional for now).

B. DB schema (SQL migration)
Create a migration (or single SQL file) with these tables (minimal MVP):

chapters(id uuid pk, name text, domain_allowlist text[])

users(id uuid pk) -- mirror Supabase auth UID

memberships(id uuid pk, user_id uuid fk users, chapter_id uuid fk chapters, role text check in ('admin','member','observer'))

pnms(id uuid pk, chapter_id uuid fk chapters, name text, year text, major text, hometown text, photo_url text, tags text[])

rounds(id uuid pk, chapter_id uuid, type text, status text, started_at timestamptz, ended_at timestamptz, room_code text)

votes(id uuid pk, round_id uuid, pnm_id uuid, voter_id uuid, score int, created_at timestamptz default now())

events(id uuid pk, chapter_id uuid, name text, starts_at timestamptz, ends_at timestamptz, location text)

event_attendance(id uuid pk, event_id uuid, pnm_id uuid, checked_in_by uuid, created_at timestamptz default now())

notes(id uuid pk, pnm_id uuid, author_id uuid, body text, tags text[], created_at timestamptz default now())

C. RLS (policies)

Enable RLS on all tables.

Policy pattern: user must have a membership row in the same chapter_id.

Admin‑only inserts/exports on sensitive tables (rounds, bulk PNM import).

Example SELECT policy on pnms (pseudocode):

using (
  exists (
    select 1 from memberships m
    where m.user_id = auth.uid()
    and m.chapter_id = pnms.chapter_id
  )
)


Example INSERT policy on votes: voter must be a member of the chapter of the round; voter_id = auth.uid().

D. Replit env & config
Set these secrets:

SUPABASE_URL

SUPABASE_ANON_KEY

SUPABASE_SERVICE_ROLE_KEY (server‑only)

SUPABASE_JWKS_URL (the /auth/v1/keys URL)

DATABASE_URL (if using direct Postgres connection)

E. FastAPI auth & DB

Add middleware to: read Authorization: Bearer, validate against JWKS, set request.state.user_id.

Add dependency get_current_user() returning {user_id} or 401.

Add data‑access helpers to enforce chapter scoping (e.g., join via memberships).

F. Proof endpoints

GET /api/me → return { user_id }.

GET /api/pnms?chapter_id=... → return PNMs scoped by membership.

POST /api/rounds/{round_id}/votes → body { pnm_id, score }, check membership and that round belongs to same chapter; insert vote.

G. Tests

Unit test for token verification (happy & invalid path).

Endpoint tests ensuring non‑members get 403.

Smoke test inserting a vote then listing results.

Review & test before accepting

After implementing, run migrations, start server, hit endpoints with and without a valid token.

Provide curl examples for each endpoint.

Summarize what was changed, any TODOs, and next recommended tasks.