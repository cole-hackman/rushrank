🔥 Step 1: Update Brand Accent (Green → Orange Gradient)

Replace bg-green-500 (or custom green tokens) with something like:

:root {
  --rr-accent: linear-gradient(135deg, hsl(25, 95%, 55%), hsl(15, 90%, 50%));
  --rr-accent-solid: hsl(25, 95%, 55%);
}


In Tailwind, extend theme:

// tailwind.config.js
extend: {
  colors: {
    accent: {
      500: '#ff7a18', // mid orange
      600: '#ff5418', // darker
    }
  }
}


Button style:

<a 
  href="/signup"
  className="rounded-full px-4 py-2 text-sm font-medium text-white 
             bg-gradient-to-r from-orange-500 to-orange-600 shadow-md hover:opacity-90"
>
  Get Started
</a>


CTA row:

“Create Chapter” → orange gradient button (primary)

Remove “See Demo”

Keep “Learn More” → styled as subtle outline

🔥 Step 2: Unify “Learn More” Page

The second page screenshot looks like it defaults to unstyled shadcn/Radix (gray backgrounds, plain text). To fix:

Apply same layout skeleton as homepage:

Sticky nav

Max width container (max-w-6xl mx-auto px-4)

Headings with same font + weight (Space Grotesk bold)

Card style with rounded-2xl bg-white/5 border border-white/10

Example component for Learn More sections:

function InfoSection({title, desc, icon}) {
  return (
    <div className="rounded-2xl p-6 bg-white/5 border border-white/10 shadow">
      <div className="flex items-center gap-3">
        <div className="h-10 w-10 rounded-xl bg-orange-500/20 flex items-center justify-center text-orange-400">
          {icon}
        </div>
        <h3 className="text-xl font-semibold text-white">{title}</h3>
      </div>
      <p className="mt-3 text-white/70 text-sm">{desc}</p>
    </div>
  );
}


Then structure “Learn More” like the homepage:

Hero → “Why RushRank?” (big headline, orange CTA)

3–4 cards (Attendance, Voting, Analytics, Security)

How It Works (step list matching homepage style)

Call to Action (email or signup)

🔥 Step 3: Consistency Checklist

✅ Typography: Use the same font pairing (Space Grotesk + Inter).
✅ Colors: Replace green with orange gradient everywhere.
✅ Buttons: Rounded, pill-shaped, same hover effect.
✅ Sections: All use surface cards with bg-white/5 + border-white/10.
✅ Spacing: Same vertical rhythm as homepage (py-20 for sections).
✅ Animations: Subtle Framer fade/slide on section load.

👉 Quick fix for now:

Swap your current “Learn More” page containers with the same <section className="mx-auto max-w-6xl px-4 py-20"> wrappers used on the homepage.

Reuse your FeatureCards + HowItWorks components (from homepage) inside Learn More for instant consistency.