You’re super close. To get that clean, “social‑app” feel (think X), you need three things:

1. a tight **design system** (tokens, type, spacing, shadows)
2. **dark/light theming** that’s truly consistent
3. a few **mobile patterns** (sticky header, bottom nav, card feed, sheet modals)

Below is a drop‑in plan + code you can hand to the Replit agent.

---

# A) Design System (tokens + type)

### 1) Tailwind + CSS variables (single source of truth)

**tailwind.config.ts**

```ts
import type { Config } from 'tailwindcss'

export default {
  darkMode: ['class'], // IMPORTANT: use class strategy
  content: ['./index.html','./src/**/*.{ts,tsx,js,jsx}'],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter var', 'system-ui', 'Segoe UI', 'Roboto', 'Helvetica Neue', 'Arial', 'Noto Sans', 'Apple Color Emoji', 'Segoe UI Emoji'],
      },
      colors: {
        // semantic tokens reference CSS vars
        bg: 'hsl(var(--bg))',
        card: 'hsl(var(--card))',
        pop: 'hsl(var(--pop))',           // brand
        text: 'hsl(var(--text))',
        textDim: 'hsl(var(--text-dim))',
        stroke: 'hsl(var(--stroke))',
        success: 'hsl(var(--success))',
        warn: 'hsl(var(--warn))',
        danger: 'hsl(var(--danger))',
      },
      boxShadow: {
        sm2: '0 1px 2px rgba(0,0,0,.08)',
        md2: '0 2px 12px rgba(0,0,0,.14)',
      },
      borderRadius: {
        xl2: '1.25rem',
      },
      spacing: { '4.5': '1.125rem' } // nice 8px-ish rhythm
    }
  },
  plugins: [],
} satisfies Config
```

**globals.css**

```css
:root {
  /* Light */
  --bg: 220 20% 98%;
  --card: 0 0% 100%;
  --pop: 221 83% 53%;           /* brand blue */
  --text: 224 71% 4%;
  --text-dim: 224 10% 40%;
  --stroke: 220 14% 90%;
  --success: 160 84% 39%;
  --warn: 38 92% 50%;
  --danger: 0 84% 60%;
}

.dark {
  /* Dark (true dark, not gray-on-black mush) */
  --bg: 220 18% 8%;
  --card: 220 14% 12%;
  --pop: 221 83% 56%;           /* slightly brighter in dark */
  --text: 0 0% 100%;
  --text-dim: 220 10% 65%;
  --stroke: 220 10% 22%;
  --success: 160 84% 44%;
  --warn: 38 92% 56%;
  --danger: 0 84% 64%;
}

/* Global reset-ish */
html, body, #root { height: 100%; }
body {
  background: hsl(var(--bg));
  color: hsl(var(--text));
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  font-feature-settings: "cv02","cv03","cv04","cv11"; /* Inter */
}
```

> Why this fixes your “font issues”:
> • class‑based dark mode prevents Tailwind from guessing the theme
> • all colors go through tokens → no orphan hex codes; contrast is tuned for both themes
> • font smoothing + Inter variable font keeps text crisp in dark mode

### 2) Type scale (social‑app tightness)

* Title: `text-xl font-semibold` (mobile), `text-2xl` (desktop)
* Section heading: `text-lg font-semibold`
* Body: `text-[15px] leading-6`
* Meta: `text-sm text-textDim`

---

# B) Theming controls (user + system preference)

**ThemeToggle.tsx (framework-agnostic pseudo)**

```tsx
import { useEffect, useState } from 'react';

export default function ThemeToggle() {
  const [dark, setDark] = useState(false);

  useEffect(() => {
    const saved = localStorage.getItem('theme');
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    const activeDark = saved ? saved === 'dark' : prefersDark;
    document.documentElement.classList.toggle('dark', activeDark);
    setDark(activeDark);
  }, []);

  const toggle = () => {
    const next = !dark;
    document.documentElement.classList.toggle('dark', next);
    localStorage.setItem('theme', next ? 'dark' : 'light');
    setDark(next);
  };

  return (
    <button onClick={toggle} className="rounded-full border border-stroke px-3 py-1 text-sm text-textDim hover:bg-card">
      {dark ? 'Dark' : 'Light'}
    </button>
  );
}
```

**Important guardrails**

* Never mix raw hexes with tokens (e.g., `text-white`) on core surfaces; use `text`/`textDim`.
* For separators: `border-stroke` only.
* For cards: `bg-card shadow-sm2` (dark mode still lifts subtly).

---

# C) Social‑app patterns to add

1. **Sticky header** with chapter name and status dot
2. **Bottom nav** (mobile): Home, PNMs, Events, Results, More
3. **Card feed** for PNMs (X‑like cell)
4. **Sheet modals** for “Add PNM”, “Join Round”, “Filters” (slide up from bottom)
5. **Pull‑to‑refresh** hint (optional) and optimistic UI on votes

**BottomNav.tsx**

```tsx
export function BottomNav() {
  const Item = ({ icon, label, active }: any) => (
    <button className={`flex flex-col items-center gap-1 px-4 py-2 ${active ? 'text-pop' : 'text-textDim'}`}>
      {icon}
      <span className="text-[11px]">{label}</span>
    </button>
  );
  return (
    <nav className="fixed inset-x-0 bottom-0 z-50 border-t border-stroke bg-card/95 backdrop-blur">
      <div className="mx-auto flex max-w-md justify-between">
        <Item label="Home" />
        <Item label="PNMs" active />
        <Item label="Events" />
        <Item label="Results" />
        <Item label="More" />
      </div>
    </nav>
  );
}
```

**PNMCell.tsx (X‑like row)**

```tsx
export function PNMCell({ pnm }) {
  return (
    <div className="flex gap-3 px-4 py-3 border-b border-stroke">
      <img src={pnm.photo_url} className="h-12 w-12 rounded-full object-cover bg-stroke" />
      <div className="min-w-0 flex-1">
        <div className="flex items-center gap-2">
          <span className="font-medium">{pnm.name}</span>
          {pnm.year && <span className="text-textDim text-sm">{pnm.year} • {pnm.major}</span>}
        </div>
        {pnm.tags?.length ? (
          <div className="mt-1 flex flex-wrap gap-1.5">
            {pnm.tags.slice(0,4).map((t: string) => (
              <span key={t} className="rounded-full border border-stroke px-2 py-0.5 text-[11px] text-textDim">{t}</span>
            ))}
          </div>
        ) : null}
        <div className="mt-2 flex gap-2 text-sm">
          <button className="rounded-full border border-stroke px-3 py-1 hover:bg-card">Note</button>
          <button className="rounded-full border border-stroke px-3 py-1 hover:bg-card">Flag</button>
          <button className="rounded-full bg-pop text-white px-3 py-1 shadow-sm2">Vote</button>
        </div>
      </div>
    </div>
  );
}
```

**Primary Button class (consistent everywhere)**

```html
<!-- className -->
"inline-flex items-center justify-center rounded-full bg-pop px-4 py-2 text-white shadow-md2 hover:opacity-95 active:opacity-90 disabled:opacity-60 transition"
```

---

# D) Fix your current screens

**Problems visible in your screenshots**

* Mixed fonts and low‑contrast gray text in dark mode
* Some components still use default `white/black` instead of tokens
* Light screen uses different radius/shadows than dark
* Inconsistent paddings on cards and “Quick Actions”

**Fix checklist (agent can run through):**

* Replace all `bg-white`, `bg-black`, `text-gray-*`, `border-gray-*` with tokens (`bg-card`, `text`, `textDim`, `stroke`).
* Global wrappers:

  * page root: `min-h-screen bg-bg text-text`
  * card: `bg-card rounded-xl2 shadow-sm2 border border-stroke`
* Headings: `text-lg font-semibold` + `text` color
* Subtext: `text-sm text-textDim`
* Standard paddings: `px-4 py-3` (rows), `p-4` (cards), `p-5` (hero tiles)
* Ensure the dark mode **class is on `<html>`**, not nested.

---

# E) Home split (Admin vs Join)

**Home.tsx**

```tsx
export default function Home() {
  return (
    <main className="mx-auto max-w-md p-4 pb-24">
      <header className="sticky top-0 z-40 -mx-4 mb-3 bg-bg/80 backdrop-blur px-4 py-3 border-b border-stroke">
        <div className="flex items-center justify-between">
          <h1 className="text-xl font-semibold">RushRank</h1>
          <ThemeToggle />
        </div>
      </header>

      <section className="space-y-4">
        <div className="rounded-xl2 bg-card p-5 shadow-md2 border border-stroke">
          <h2 className="text-lg font-semibold">Rush Chair / Admin</h2>
          <p className="mt-1 text-textDim text-sm">Manage PNMs, events, and results</p>
          <button className="mt-4 inline-flex rounded-full bg-pop px-4 py-2 text-white">Sign in</button>
        </div>

        <div className="rounded-xl2 bg-card p-5 shadow-md2 border border-stroke">
          <h2 className="text-lg font-semibold">Join Voting Round</h2>
          <p className="mt-1 text-textDim text-sm">Enter the code from your rush chair</p>
          <div className="mt-3 flex gap-2">
            <input className="flex-1 rounded-full border border-stroke bg-bg px-4 py-2 outline-none" placeholder="ABC123" />
            <button className="rounded-full bg-pop px-4 py-2 text-white">Join</button>
          </div>
        </div>
      </section>

      <BottomNav />
    </main>
  );
}
```

---

# F) Motion & feel (micro‑interactions)

* **Timing:** 180ms ease‑out for hover/focus; 220ms ease‑in for dismiss
* **Tap feedback:** `active:scale-[.99]` on buttons/cards
* **Swipe vote card:** slight `rotate-1 translate-y-[1px]` before release
* **Haptics (mobile):** `navigator.vibrate?.(10)` on successful vote

Example utility: `className="transition-transform duration-150 active:scale-[.98]"`

---

# G) Accessibility & readability

* Minimum contrast 4.5:1 for text. Your tokens above are tuned for that.
* Hit targets ≥ 44×44px.
* Use `aria-pressed` on toggle buttons (e.g., tags).
* Focus rings: rely on browser default or add `focus-visible:outline focus-visible:outline-2 focus-visible:outline-pop`.

---

# H) Give this to your Replit agent (PREFIRE)

**PREFIRE PROMPT about “Design system + Dark/Light theme + Home split + PNM row”**

1. **Goal**
   Refactor UI to a social‑app style with a tokenized design system and reliable dark/light theming. Implement:

* Tailwind config + CSS variables (as provided)
* Global typography + smoothing
* Home split (Admin vs Join) using new components
* PNM list row cell (X‑like)
* Bottom nav, sticky header
* Replace all hardcoded colors with tokens

2. **Research**
   Scan codebase for components with raw hex/gray classes and inline styles; list them.

3. **Plan**

* Add `globals.css` and update `tailwind.config.ts` exactly as above
* Implement `ThemeToggle`, `BottomNav`, `PNMCell`
* Convert Dashboard, Add PNM sheet, Events, Results empty state to tokens + standardized spacings
* Ensure `<html class="dark">` toggles via localStorage + prefers-color-scheme

4. **Steps**

* Apply token classes to: header, cards, inputs, buttons
* Fix dark‑mode text contrast using `text`/`textDim`
* Uniform paddings/radii/shadows
* Test both themes across pages

5. **Review & Test**
   Screenshot light/dark versions of: Home, Dashboard (empty + with PNMs), Add PNM sheet, Events list, Results empty state. Confirm no mixed fonts/low contrast. List any files still using raw colors.

---

If you want, I can also generate a compact **Figma‑ready style sheet** (tokens, components, spacing) or expand the PNM **Vote Card** with swipe gestures next.
