[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: requires a real Postgres via TEST_DATABASE_URL (see tests/integration/conftest.py)
filterwarnings =
    ignore::DeprecationWarning
