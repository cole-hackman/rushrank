"\"\"\"\n+Pillow-based composition utilities for RushRank PNM share cards\n+\n+Instagram portrait spec: 1080x1350, top 60% photo, bottom 40% info panel.\n+\"\"\"\n+from __future__ import annotations\n+\n+from io import BytesIO\n+from typing import List, Optional\n+\n+from PIL import Image, ImageDraw, ImageFont, ImageOps\n+import httpx\n+\n+W, H = 1080, 1350\n+SAFE = 48\n+PHOTO_H = int(H * 0.60)\n+\n+# Colors\n+BG = (245, 247, 250)            # light panel bg\n+NAVY = (22, 34, 56)             # soft navy\n+TEXT = (20, 22, 26)             # primary text\n+TEXT_MUTED = (90, 96, 106)      # muted text\n+PILL_BG = (210, 219, 235)       # muted navy pill bg\n+\n+\n+def _try_load_font(path: str, size: int) -> ImageFont.FreeTypeFont:\n+    try:\n+        return ImageFont.truetype(path, size=size)\n+    except Exception:\n+        # Fallback to default PIL bitmap font (size ignored)\n+        return ImageFont.load_default()\n+\n+\n+def _load_fonts():\n+    \"\"\"Load fonts with graceful fallbacks.\n+    Looks for Inter in common repo locations; falls back to PIL default.\n+    \"\"\"\n+    candidates = [\n+        # Project-local font paths if added later\n+        \"python_server/assets/fonts/Inter-Bold.ttf\",\n+        \"assets/fonts/Inter-Bold.ttf\",\n+        \n+        # System fonts (best-effort)\n+        \"/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf\",\n+        \"/Library/Fonts/Arial Bold.ttf\",\n+    ]\n+    candidates_reg = [\n+        \"python_server/assets/fonts/Inter-Regular.ttf\",\n+        \"assets/fonts/Inter-Regular.ttf\",\n+        \"/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf\",\n+        \"/Library/Fonts/Arial.ttf\",\n+    ]\n+    candidates_med = [\n+        \"python_server/assets/fonts/Inter-Medium.ttf\",\n+        \"assets/fonts/Inter-Medium.ttf\",\n+        \"/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf\",\n+        \"/Library/Fonts/Arial.ttf\",\n+    ]\n+    candidates_sb = [\n+        \"python_server/assets/fonts/Inter-SemiBold.ttf\",\n+        \"assets/fonts/Inter-SemiBold.ttf\",\n+        \"/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf\",\n+        \"/Library/Fonts/Arial.ttf\",\n+    ]\n+\n+    def first(paths: List[str], size: int) -> ImageFont.FreeTypeFont:\n+        for p in paths:\n+            f = _try_load_font(p, size)\n+            # When load_default is returned, it has no path, but it's acceptable as a fallback\n+            if f is not None:\n+                return f\n+        return ImageFont.load_default()\n+\n+    font_name = first(candidates, 72)\n+    font_sub = first(candidates_med, 42)\n+    font_body = first(candidates_reg, 36)\n+    font_tag = first(candidates_sb, 28)\n+    font_brand = first(candidates_sb, 22)\n+    return font_name, font_sub, font_body, font_tag, font_brand\n+\n+\n+def _cover(image: Image.Image, target_w: int, target_h: int) -> Image.Image:\n+    return ImageOps.fit(image, (target_w, target_h), method=Image.Resampling.LANCZOS, centering=(0.5, 0.5))\n+\n+\n+def _wrap_by_width(draw: ImageDraw.ImageDraw, text: str, font: ImageFont.FreeTypeFont, max_width: int) -> List[str]:\n+    if not text:\n+        return []\n+    words = text.split()\n+    if not words:\n+        return []\n+    lines: List[str] = []\n+    line = \"\"\n+    for word in words:\n+        test = (line + \" \" + word).strip()\n+        bbox = draw.textbbox((0, 0), test, font=font)\n+        tw = bbox[2] - bbox[0]\n+        if tw <= max_width:\n+            line = test\n+        else:\n+            if line:\n+                lines.append(line)\n+            line = word\n+    if line:\n+        lines.append(line)\n+    return lines\n+\n+\n+async def _load_remote_image(url: str) -> Optional[Image.Image]:\n+    try:\n+        async with httpx.AsyncClient(timeout=10) as client:\n+            resp = await client.get(url)\n+            resp.raise_for_status()\n+            img = Image.open(BytesIO(resp.content)).convert(\"RGB\")\n+            return img\n+    except Exception:\n+        return None\n+\n+\n+async def compose_pnm_card(\n+    name: str,\n+    hometown: str,\n+    major: str,\n+    fun_fact: Optional[str],\n+    photo_url: Optional[str],\n+    tags: Optional[List[str]],\n+    brand_text: str = \"RushRank\",\n+) -> bytes:\n+    # Base image\n+    img = Image.new(\"RGB\", (W, H), \"white\")\n+    draw = ImageDraw.Draw(img)\n+\n+    font_name, font_sub, font_body, font_tag, font_brand = _load_fonts()\n+\n+    # Top photo area\n+    photo: Optional[Image.Image] = None\n+    if photo_url:\n+        photo = await _load_remote_image(photo_url)\n+    if photo is not None:\n+        p = _cover(photo, W, PHOTO_H)\n+        img.paste(p, (0, 0))\n+    else:\n+        # Placeholder block; could include initials if desired\n+        draw.rectangle([(0, 0), (W, PHOTO_H)], fill=(230, 233, 238))\n+\n+    # Add a subtle black->transparent gradient overlay at bottom of the photo to aid contrast\n+    grad_h = 180\n+    gradient = Image.new(\"RGBA\", (W, grad_h), (0, 0, 0, 0))\n+    gdraw = ImageDraw.Draw(gradient)\n+    for i in range(grad_h):\n+        alpha = int(200 * (i / grad_h))  # 0 -> 200\n+        gdraw.line([(0, i), (W, i)], fill=(0, 0, 0, alpha))\n+    img.paste(gradient, (0, PHOTO_H - grad_h), gradient)\n+\n+    # Bottom info panel\n+    draw.rectangle([(0, PHOTO_H), (W, H)], fill=BG)\n+\n+    x = SAFE\n+    y = PHOTO_H + SAFE\n+\n+    # Name\n+    draw.text((x, y), name, font=font_name, fill=TEXT)\n+    y += getattr(font_name, \"size\", 64) + 12\n+\n+    # Subhead: hometown • major\n+    sub = f\"{hometown or '—'}  •  {major or '—'}\"\n+    draw.text((x, y), sub, font=font_sub, fill=TEXT_MUTED)\n+    y += getattr(font_sub, \"size\", 36) + 20\n+\n+    # Tag pills\n+    if tags:\n+        tx = x\n+        pill_pad_x, pill_pad_y = 18, 10\n+        gap = 12\n+        for tag in list(tags)[:6]:\n+            label = (tag or \"\").upper()\n+            if not label:\n+                continue\n+            tb = draw.textbbox((0, 0), label, font=font_tag)\n+            tw = tb[2] - tb[0]\n+            th = tb[3] - tb[1]\n+            pill_w = tw + pill_pad_x * 2\n+            pill_h = th + pill_pad_y * 2\n+            if tx + pill_w > W - SAFE:\n+                y += pill_h + 10\n+                tx = x\n+            draw.rounded_rectangle([(tx, y), (tx + pill_w, y + pill_h)], radius=14, fill=PILL_BG)\n+            draw.text((tx + pill_pad_x, y + pill_pad_y), label, font=font_tag, fill=NAVY)\n+            tx += pill_w + gap\n+        y += pill_h + 22 if 'pill_h' in locals() else 0\n+\n+    # Fun fact\n+    if fun_fact:\n+        draw.text((x, y), \"Fun fact\", font=font_sub, fill=NAVY)\n+        y += getattr(font_sub, \"size\", 36) + 8\n+        maxw = W - SAFE - x\n+        for line in _wrap_by_width(draw, fun_fact, font_body, maxw):\n+            draw.text((x, y), line, font=font_body, fill=TEXT)\n+            y += getattr(font_body, \"size\", 32) + 6\n+\n+    # Brand/footer\n+    brand_bbox = draw.textbbox((0, 0), brand_text, font=font_brand)\n+    brand_w = brand_bbox[2] - brand_bbox[0]\n+    brand_h = brand_bbox[3] - brand_bbox[1]\n+    draw.text((W - SAFE - brand_w, H - SAFE - brand_h), brand_text, font=font_brand, fill=TEXT_MUTED)\n+\n+    out = BytesIO()\n+    img.save(out, format=\"PNG\", optimize=True)\n+    return out.getvalue()\n+\n*** End Patch

